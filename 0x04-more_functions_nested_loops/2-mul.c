#include "main.h"

/**
 * mul -this function multiplies two integers
 * @a: multiplied with b
 * @b: multiplied with a
 * Return: this returns the multiplied value in a new variable c
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
