#include <stdio.h>

/**
 * main - The C function prints out the following string
 * with proper grammar, but the outcome is a piece of art,
 * Return: This function returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
